[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: fast, isolated tests of a single function or class
    integration: tests exercising multiple components together (the graph, the API)
